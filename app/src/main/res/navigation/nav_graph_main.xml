<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph_main"
    app:startDestination="@id/mainFragment">

    <fragment
        android:id="@+id/mainFragment"
        android:name="com.truedigital.workshop.MainFragment"
        android:label="Main Fragment"
        tools:layout="@layout/fragment_main">
        <action
            android:id="@+id/action_mainFragment_to_meActivity"
            app:destination="@id/meActivity" />
    </fragment>

    <activity
        android:id="@+id/meActivity"
        android:name="com.truedigital.navigation.me.MeActivity"
        android:label="Me Activity"
        tools:layout="@layout/activity_me" />

</navigation>